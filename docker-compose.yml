services:

  # ─── O3 Reference Application ───────────────────────────────────────

  gateway:
    image: openmrs/openmrs-reference-application-3-gateway:${O3_TAG:-qa}
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - "9096:80"
    deploy:
      resources:
        limits:
          memory: 64m
          cpus: '0.25'

  frontend:
    image: openmrs/openmrs-reference-application-3-frontend:${O3_TAG:-qa}
    restart: unless-stopped
    environment:
      SPA_PATH: /openmrs/spa
      API_URL: /openmrs
      SPA_CONFIG_URLS: /openmrs/spa/config-core_demo.json
      SPA_DEFAULT_LOCALE:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      timeout: 5s
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 128m
          cpus: '0.25'

  backend:
    image: openmrs/openmrs-reference-application-3-backend:${O3_TAG:-qa}
    restart: unless-stopped
    depends_on:
      - db
    environment:
      OMRS_CONFIG_MODULE_WEB_ADMIN: "true"
      OMRS_CONFIG_AUTO_UPDATE_DATABASE: "true"
      OMRS_CONFIG_CREATE_TABLES: "true"
      OMRS_CONFIG_CONNECTION_SERVER: db
      OMRS_CONFIG_CONNECTION_DATABASE: openmrs
      OMRS_CONFIG_CONNECTION_USERNAME: ${OMRS_DB_USER:-openmrs}
      OMRS_CONFIG_CONNECTION_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
      JAVA_TOOL_OPTIONS: "-Xms512m -Xmx1536m -XX:+UseG1GC -XX:MaxMetaspaceSize=256m"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/openmrs"]
      timeout: 5s
    volumes:
      - openmrs-data:/openmrs/data
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2.00'

  db:
    image: mariadb:10.11.7
    restart: unless-stopped
    command: "mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci"
    healthcheck:
      test: "mysql --user=${OMRS_DB_USER:-openmrs} --password=${OMRS_DB_PASSWORD:-openmrs} --execute \"SHOW DATABASES;\""
      interval: 3s
      timeout: 1s
      retries: 5
    environment:
      MYSQL_DATABASE: openmrs
      MYSQL_USER: ${OMRS_DB_USER:-openmrs}
      MYSQL_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-openmrs}
    volumes:
      - db-data:/var/lib/mysql
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '0.50'

  # ─── CCE Receiver Adaptor ──────────────────────────────────────────

  openmrs-cce-receiver-adaptor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openmrs-cce-receiver-adaptor
    ports:
      - "8084:8080"
    depends_on:
      backend:
        condition: service_healthy
    environment:
      OPENMRS_REST_BASE_URL: ${OPENMRS_REST_BASE_URL:-http://gateway:80/openmrs/ws/rest/v1}
      OPENMRS_FHIR_BASE_URL: ${OPENMRS_FHIR_BASE_URL:-http://gateway:80/openmrs/ws/fhir2/R4}
      OPENMRS_REST_TIMEOUT: "30000"
      OPENMRS_FHIR_TIMEOUT: "30000"
      OPENMRS_AUTH_TYPE: basic
      OPENMRS_USERNAME: ${OPENMRS_USERNAME:-admin}
      OPENMRS_PASSWORD: ${OPENMRS_PASSWORD:-Admin123}
      OPENMRS_IDENTIFIER_LOCATION_UUID: ""
      OPENMRS_IDGEN_SOURCE_UUID: ""
      OPENMRS_IDENTIFIER_TYPE_NAME: "OpenMRS ID"
      OPENMRS_CAPABILITY_REFRESH_MS: "21600000"
      CCE_SECURITY_ENABLED: "false"
      CCE_SECURITY_TOKEN: ""
      LOG_LEVEL: INFO
      JAVA_OPTS: "-Xms256m -Xmx512m"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  openmrs-data: ~
  db-data: ~
